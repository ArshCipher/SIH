# Optional NLP libs (handled as optional in code) – intentionally omitted for Py3.13 ease
# transformers
# torch
# spacy
nltk==3.8.1

# Web framework and API
fastapi==0.115.0
uvicorn==0.31.0
pydantic==2.9.2

# Database and data handling
sqlalchemy==2.0.35
# pandas 2.x may lack wheels on very new Python versions; omit for now
requests==2.31.0

# WhatsApp and SMS integration
twilio==8.10.3

# Translation and multilingual support
# Make googletrans optional; comment out by default to avoid Python 3.13 httpx cgi issue
# googletrans==4.0.0rc1
langdetect==1.0.9

# Monitoring and analytics
prometheus-client==0.20.0
# plotting libs optional; omit to avoid heavy native deps on Py3.13

# Environment and configuration
python-dotenv==1.0.1
pydantic-settings==2.4.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Development tools
black==24.8.0
flake8==7.1.1
